[package]
name = "aetheris_scan"
version = "0.1.0"
edition = "2021"
description = "Aetheris native scan helpers — entropy + byte-pattern search (C ABI)"

[lib]
name = "aetheris_scan"
crate-type = ["cdylib"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
